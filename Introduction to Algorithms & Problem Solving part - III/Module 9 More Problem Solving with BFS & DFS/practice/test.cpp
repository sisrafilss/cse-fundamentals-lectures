#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    pair<int, int> test = {-1, -1};

    if (test == make_pair(-1, -1))
    {
        printf("same\n");
    }
    else
    {
        printf("not same\n");
    }

    return 0;
}