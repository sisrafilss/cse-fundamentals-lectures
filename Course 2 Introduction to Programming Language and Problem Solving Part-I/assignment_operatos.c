#include<stdio.h>

int main() {
    int price = 20;
    printf("price = %d\n", price);

    // price = price + 10;
    // price += 16;
       price++;

    printf("price = %d\n", price);

    return 0;
}
